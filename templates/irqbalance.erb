# managed by puppet

#Configuration for the irqbalance daemon

#Should irqbalance be enabled?
ENABLED="<%= @enabled %>"
#Balance the IRQs only once?
ONESHOT="<%= @oneshot %>"

OPTIONS="<%= @options %>"
